#include <stdio.h>
/**
 * main - Print all the letters except q and e
 *
 *  Return: 0
 */
int main(void)
{
	int n;

	for (n = 'a'; n <= 'z'; n++)
	{
		if (n != 'q' && n != 'e')
	}
	putchar('\n');
	return (0);
}
